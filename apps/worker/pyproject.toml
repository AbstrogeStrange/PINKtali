[tool.poetry]
name = "streamverse-worker"
version = "0.1.0"
description = "Transcoding worker for StreamVerse"
authors = ["StreamVerse <admin@streamverse.com>"]

[tool.poetry.dependencies]
python = "^3.12"
bullmq = "^1.14.0"
ffmpeg-python = "^0.2.0"
boto3 = "^1.34.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
